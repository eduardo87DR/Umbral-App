name: my_dungeon_admin
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_riverpod: ^2.3.6
  http: ^0.13.6
  flutter_secure_storage: ^8.0.0
  intl: ^0.18.0
  flutter_local_notifications: ^12.0.3

flutter:
  uses-material-design: true
